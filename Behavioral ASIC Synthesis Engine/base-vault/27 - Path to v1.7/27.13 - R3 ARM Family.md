---
created: 2026-07-18
updated: 2026-07-18
tags: [v1.7, r3, arm, aarch64]
---

# R3 — ARM family

## Entregas

| Item | Estado |
|------|--------|
| Goldens `arm_nop_ret` / `arm_add3` | ✅ |
| Goldens `aarch64_nop_ret` / `aarch64_add3` | ✅ |
| `assemble_arm` + CLI `base recomp assemble-arm` | ✅ (toolchain `arm-none-eabi-as`) |
| AArch64 assemble live | ⏳ golden-only até `aarch64-*-as` no PATH |

## Demo

```bash
cargo test -p base-recomp --test r3_arm_family
base recomp assemble-arm --hex B8010000000502000000C3 --name add3 -o output/r3
base recomp lift --hex 90c3 --target aarch64 -o output/r3_a64
```

## Honesty

Emit subset ≠ ABI AAPCS completo · assemble ≠ executar no device · `static_recomp_complete: false`.

[[27.00 - Index]] · [[27.04 - Sprint Board]]
