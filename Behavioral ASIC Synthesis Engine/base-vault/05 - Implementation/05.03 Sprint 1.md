---
tags:
  - implementation
  - sprint
  - completed
---

# Sprint 1 — Inference Core ✅

## Objetivo

Construir o motor de inferência comportamental (base-core).

## Tarefas

- [x] Implementar Block Extraction (Stage 1): clustering por página, classificação de tipo
- [x] Implementar Protocol Inference (Stage 2): N-gram analysis, detecção de sequências
- [x] Implementar FSM Extraction (Stage 3): graph mining, matriz de latência
- [x] Implementar Specification Generator (Stage 4): conversão para HardwareSpec YAML
- [x] Definir schema do Component DB

## Resultado

| Módulo | Arquivos | Status |
|--------|----------|--------|
| spec/types.rs | Tipos centrais | ✅ `HardwareSpec`, `FunctionalBlock`, `Register`, `Protocol`, `TimingProfile`, serialização YAML/JSON |
| inference/extraction.rs | Stage 1 | ✅ `extract_blocks()` — agrupa por página, classifica por tipo, extrai registradores |
| inference/protocol.rs | Stage 2 | ✅ `infer_protocol()` — N-gram analysis, papéis de registradores, nomes heurísticos |
| inference/fsm.rs | Stage 3 | ✅ `extract_fsm()` — estados de control register, transições, matriz de latência |
| inference/generator.rs | Stage 4 | ✅ `generate_spec()` — pipeline completo: clusters → protocolo → FSM → HardwareSpec YAML |
| component_db/mod.rs | Component DB | ✅ Schema + `ComponentDb` com load de YAML, busca por categoria/periférico |

## Critérios de Aceite

1. ✅ Pipeline de 4 estágios executando de ponta a ponta — `generate_spec()` integra todos
2. ✅ Inferência de FSM com implementação funcional
3. ✅ HardwareSpec YAML gerado com confidence score — `to_yaml()` + `calculate_confidence_raw()`

## Dependências

- ✅ [[05.02 Sprint 0]] — SpecterProbe completo

## Integração

Output alimenta [[05.04 Sprint 2 — Hardware Mapping]].
