# Firmware no pacote hardware

Opcional. Firmware de produção/lab do wedge vive nos crates e `examples/` do repositório.
Use este diretório só para firmware **específico da placa de referência** (bring-up, blink, USB enum).

Honesty: flash = lab assist / manual — ver `base-hil` e SOW Claim A.
