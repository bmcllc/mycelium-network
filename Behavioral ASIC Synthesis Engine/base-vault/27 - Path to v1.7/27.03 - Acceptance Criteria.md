---
created: 2026-07-18
updated: 2026-07-18
tags: [acceptance, v1.7]
---

# Acceptance Criteria — Path to v1.7

## R0 — Foundation

- [x] Notas 27.00–27.11 + playbook no `base-vault/`
- [x] `base-recomp` no workspace
- [x] `TargetIsa` cobre todos os alvos (amd64 alias de x86_64)
- [x] Honesty: `static_recomp_complete: false` · `win32_abi_complete: false`
- [x] `cargo test -p base-recomp` verde

## R1 — Lift

- [x] Lifter x86-32 subset documentado
- [x] Gap/`Unknown` quando opcode não suportado
- [x] CLI `base recomp lift` / `file` / `targets`

## R2 — x86_64

- [x] Emit SIR → ASM x86_64 golden tests (`tests/goldens/x86_64_*.s`)
- [x] Roundtrip smoke host (`as`/`cc`) — `base recomp roundtrip` + `roundtrip::smoke_x86_64`

## R3 — ARM family

- [x] Emit ARM + AArch64 subset com goldens (`arm_*.s`, `aarch64_*.s`)
- [x] Assemble-only ARM via `arm-none-eabi-as` (`base recomp assemble-arm`) — ≠ qemu run · AArch64 assemble = golden-only até toolchain

## R4 — Classic RISC

- [x] Emit MIPS + PPC + SPARC subset com goldens
- [x] Aliases CLI `mips` / `ppc|powerpc` / `sparc` cobertos em teste
- [ ] Assemble live (toolchain não presente no lab — golden-only)

## R5 — SuperH + RC

- [x] Emit SH-2 e SH-4 subset (`sh_*.s` goldens + flavor banners)
- [x] Playbook + Path v1.7 fechado no vault · honesty banner intacto (`static_recomp_complete: false`)
- [x] Tag git `v1.7.0-rc` — release Path to v1.7

[[27.00 - Index]]
