---
tags:
  - implementation
  - sprint
  - completed
---

# Sprint 5 — Validation ✅

## Objetivo

Comparar automaticamente o comportamento do hardware original com o gerado.

## Tarefas

- [x] Parser de traces Saleae CSV
- [ ] Parser de traces Wireshark PCAP — **❌ NÃO IMPLEMENTADO**
- [x] Alinhamento temporal de traces (correlação)
- [x] Comparador de operações (MMIO, IRQ, DMA)
- [x] Agregação de métricas
- [x] Gerador de relatório HTML + JSON
- [ ] Modos: estático, replay simulado, replay HW — **❌ NÃO IMPLEMENTADO**

## Resultado

| Módulo | Arquivos | Status |
|--------|----------|--------|
| tracer | `base-check/src/tracer.rs` | ✅ Saleae CSV (`Time,CH,Type,Data`) + JSON custom — ❌ Wireshark PCAP ausente |
| aligner | `base-check/src/aligner.rs` | ✅ Sliding window alignment com `max_time_delta_ns`, `alignment_stats()` |
| compare | `base-check/src/compare.rs` | ✅ `OperationComparator::compare()` — endereço/valor/latência, `group_by_type()` |
| metrics | `base-check/src/metrics.rs` | ✅ `ValidationThresholds` (5 limiares) + `aggregate_metrics()` (pass rate, latência, acurácia, warnings) |
| report | `base-check/src/report.rs` | ✅ `ReportGenerator::generate_html()` (CSS dark) + `generate_json()` |

## Critérios de Aceite

1. ⚠️ Parse de traces Saleae ✅ / Wireshark ❌
2. ⚠️ Comparação com sliding window — implementada, taxa de acerto depende dos dados
3. ⚠️ HTML com tabelas estilizadas — sem gráficos SVG (sem `plotters`)
4. ✅ Thresholds configuráveis — global, sem discriminação por tipo de bloco

## Itens Pendentes

- [ ] Parser Wireshark PCAP — precisa de crate `pcap` ou parser manual
- [ ] Modos de validação — estático, replay simulado, replay HW
- [ ] Gráficos no relatório HTML — integrar `plotters` ou gerar SVG inline
- [ ] Thresholds por tipo de bloco — GPU vs Audio vs DMA com tolerâncias diferentes

## Dependências

- ✅ [[05.05 Sprint 3]] e [[05.06 Sprint 4]] — PCB e FW gerados
- externa: `csv` ✅, `plotters` ❌ (não adicionado)

## Integração

Output alimenta [[05.08 Sprint 6 — Evolution]].
