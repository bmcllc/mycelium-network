---
created: 2026-07-17
updated: 2026-07-17
tags: [v1.4, usb, bringup, g35]
---

# G35 — USB × DTB + atlas P0 absoluto

## Alvo de port

**`linux_wedge_uart_ufs_g35`** — early bring-up AArch64 no ums9620 (UART → arch timer/GIC → UFS).  
GPU/display/modem = depois. ≠ TaurOS turnkey · `generates_os: false`.

## Atlas P0 (`wedge_mmio_map.yaml`)

Combina **USB absolutos** (`/sys/bus/platform/devices`) com **DT `@unit`** físicos (≥ `0x01000000`).  
Não é walk completo de `ranges` FDT — honesty: confirmar se o bus traduz.

Esperado no G35 real:

| Class | Absolute | Source |
|-------|----------|--------|
| uart | `0x20200000` | USB |
| gic | `0x12000000` | DT `@12000000` |
| storage_emmc_ufs | `0x22000000` | USB |
| arm_generic_timer | — | system-reg CNT* |

## Comandos

```bash
./examples/pilot_moto_g35/run_usb_probe.sh
./examples/pilot_moto_g35/run_usb_cross.sh
# → out_real/usb_cross/wedge_mmio_map.yaml
# → out_real/usb_cross/BRINGUP_CHECKLIST.md
```

## Ordem P0→P2

1. UART `0x20200000`  
2. Arch timer + GIC `0x12000000`  
3. UFS `0x22000000`  
4. GPIO/pinctrl  
5. GPU/DPU / USB3 (opcional)

Cruzar com `port_package_lk/` (fósseis / must rewrite).

[[24.40 - Port Package]] · [[24.20 - Forensic Playbook]]
