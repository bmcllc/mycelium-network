---
created: 2026-07-16
updated: 2026-07-16
tags: [playbook, forense, v1.0]
aliases: [Forensic Playbook v1.0]
---

# Playbook forense — 1 página (v1.0)

**Objetivo:** Evidence → contratos → Design (RP + STM32 USART/SPI2/I2C1/TIM2) + goldens `diff` (RP/USART/SPI/I2C) + smokes multi-peripheral + reconstruct honesto + HIL CLI EXPERIMENTAL; engenheiro no loop.

> **v1.0 ≠** produto industrial completo / ASIC drop-in / SaaS turnkey.

## Setup (15–20 min)

```bash
git clone https://github.com/bmcc-DEV/B.A.S.E..git
cd B.A.S.E.
cargo build -p base-cli

# Gates permanentes (RP goldens = diff, sem overwrite)
./examples/pilot/run.sh
./examples/pilot/run_t1_b2.sh

# STM32 USART (Capstone + pins + goldens)
./examples/pilot_stm32/run.sh

# STM32 dual USART+SPI2 + pins PB13/14/15 + goldens expected_spi/
./examples/pilot_stm32/run_w1_spi.sh

# STM32 dual USART+I2C1 + pins PB6/PB7 + goldens expected_i2c/
./examples/pilot_stm32/run_x3_i2c.sh

# STM32 triple USART+SPI2+I2C1
./examples/pilot_stm32/run_y3_triple.sh

# STM32 dual USART+TIM2 @ 0x40000000 (Z2)
./examples/pilot_stm32/run_z2_tim.sh

# Reconstruct (estagnação honesta — ≠ auto-fix)
base reconstruct examples/pilot_stm32/out/analyze/hardware_spec.yaml \
  --continuous --threshold 0.99 -o /tmp/recon_demo

# HIL CLI (host-only)
base hil enumerate -o /tmp/hil_demo
base hil flash /dev/null --mock-flash -o /tmp/hil_demo
```

## Mostrar ao cliente

1. **RP** — UART + SPI; **goldens** `diff` vs `expected/`.
2. **STM32 USART** — Capstone; PA9/PA10; goldens STM32.
3. **STM32 SPI2** — `@ 0x40003800` + PB13/14/15 + goldens `expected_spi/` (`diff`).
4. **STM32 I2C1** — `@ 0x40005400` + PB6/PB7 + goldens `expected_i2c/` (`diff`).
5. **Triple** — classify 3 páginas (`run_y3_triple.sh`); design ST → `STM32F103C8`.
6. **TIM2** — `@ 0x40000000` + classify `timer`/`tim` (`run_z2_tim.sh`); 4º tipo opt-in.
7. **Reconstruct** — `stop_reason` / estagnação; **não** auto-fix.
8. **HIL CLI** — Simulated / `--mock-flash`; nunca production.
9. **Maturity Matrix** — fonte da verdade do wedge ([[12 - Path to Real/12.02 - Maturity Matrix]]).

Case: [[12 - Path to Real/12.20 - Pilot Case Study]] · plano [[20.00 - Index]] · SOW [[20.21 - SOW Industrial Checklist]] · continuous [[17 - Path to v0.7/17.30 - Reconstruct Continuous]].

## Não dizer

- ASIC drop-in / PCB fabricável / HIL production / SaaS turnkey
- “v1.0 = produto industrial completo”
- “Capstone AArch64 = firmware Thumb do Blue Pill”
- “STM32 substituiu o gate RP”
- “`--continuous` = auto-fix completa”
- “SPI1 dual com USART1” (colisão 4K)
- “Triple / TIM2 = PCB fabricável / silício validado”

## Oferta

Viabilidade (1–2 wedges) + handoff → [[20.21 - SOW Industrial Checklist|SOW v1.0]].

[COMMERCIAL.md](../../../COMMERCIAL.md) · ← [[20.14 - Sprint Z4 Offer]]
