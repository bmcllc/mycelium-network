---
created: 2026-07-16
updated: 2026-07-16
tags: [playbook, forense, v0.8]
aliases: [Forensic Playbook v0.8]
---

> **v0.9:** use [[19 - Path to v0.9/19.20 - Forensic Playbook|Playbook v0.9]].

# Playbook forense — 1 página (v0.8)

**Objetivo:** Evidence → contratos → Design (RP + STM32 USART/SPI2/I2C1) + goldens `diff` + reconstruct honesto + HIL CLI EXPERIMENTAL; engenheiro no loop.

## Setup (12–15 min)

```bash
git clone https://github.com/bmcc-DEV/B.A.S.E..git
cd B.A.S.E.
cargo build -p base-cli

# Gates permanentes (RP goldens X2 = diff, sem overwrite)
./examples/pilot/run.sh
./examples/pilot/run_t1_b2.sh

# STM32 USART (Capstone + pins + goldens)
./examples/pilot_stm32/run.sh

# STM32 dual USART+SPI2 + pins PB13/14/15 (X1)
./examples/pilot_stm32/run_w1_spi.sh

# STM32 dual USART+I2C1 (X3)
./examples/pilot_stm32/run_x3_i2c.sh

# Reconstruct (estagnação honesta — ≠ auto-fix)
base reconstruct examples/pilot_stm32/out/analyze/hardware_spec.yaml \
  --continuous --threshold 0.99 -o /tmp/recon_demo

# HIL CLI (host-only)
base hil enumerate -o /tmp/hil_demo
base hil flash /dev/null --mock-flash -o /tmp/hil_demo
```

## Mostrar ao cliente

1. **RP** — UART + SPI; **goldens** `diff` vs `expected/` (X2).
2. **STM32 USART** — Capstone; PA9/PA10; goldens STM32.
3. **STM32 SPI2** — `@ 0x40003800` + labels PB13/14/15 `NOT FABRICABLE`.
4. **STM32 I2C1** — `@ 0x40005400` opt-in (`run_x3_i2c.sh`).
5. **Reconstruct** — `stop_reason` / estagnação; **não** auto-fix.
6. **HIL CLI** — Simulated / `--mock-flash`; nunca production.

Case: [[12 - Path to Real/12.20 - Pilot Case Study]] · plano [[18.00 - Index]] · SOW [[18.21 - SOW Industrial Checklist]] · continuous [[17 - Path to v0.7/17.30 - Reconstruct Continuous]].

## Não dizer

- ASIC drop-in / PCB fabricável / HIL production / SaaS turnkey
- “Capstone AArch64 = firmware Thumb do Blue Pill”
- “STM32 substituiu o gate RP”
- “`--continuous` = auto-fix completa”
- “SPI1 dual com USART1” (colisão 4K)

## Oferta

Viabilidade (1–2 wedges) + handoff → [[18.21 - SOW Industrial Checklist|SOW v0.8]].

[COMMERCIAL.md](../../../COMMERCIAL.md) · ← [[18.14 - Sprint X4 Offer]]
