---
tags:
  - architecture
  - diagram
---

# Component Diagram

```mermaid
graph TB
    subgraph "SpecterProbe Foundation"
        ACQ[acquisition<br/>Firmware aquisition<br/>DTB parser<br/>ADB probe]
        LIFT[lift<br/>Disassembly<br/>CFG reconstruction<br/>LLVM IR]
        MMIO[mmio<br/>MMIO scanner<br/>Region classifier]
        BEH[behavior<br/>Register map<br/>FSM inference<br/>Bitfield detection]
        COMP[compat<br/>GPU/ISP/DSP<br/>detection]
        GENOME[genome<br/>Device genome<br/>YAML export]
        KNOW[knowledge<br/>Knowledge graph<br/>Neo4j export]
        GENERATE[generate<br/>Redox drivers<br/>QEMU devices]
    end
    
    subgraph "B.A.S.E. Core"
        CLI[base-cli<br/>CLI unificada]
        CORE[base-core<br/>HardwareSpec<br/>Component mapping<br/>Constraint solving]
        PCB[base-pcb<br/>KiCad schematic<br/>Netlist<br/>BOM<br/>PCB layout]
        FW[base-fw<br/>Bootloader gen<br/>HAL gen<br/>Driver gen<br/>BIOS shim]
        CHECK[base-check<br/>Trace replay<br/>Comparison engine<br/>Metrics]
        EVOLVE[base-evolve<br/>Bottleneck analysis<br/>Upgrade suggestions]
        DB[Component DB<br/>YAML catalog<br/>50+ components]
    end
    
    ACQ --> LIFT
    LIFT --> MMIO
    MMIO --> BEH
    MMIO --> COMP
    BEH --> GENOME
    COMP --> GENOME
    BEH --> GENERATE
    GENOME --> CORE
    COMP --> CORE
    KNOW --> CORE
    
    CORE --> DB
    CORE --> PCB
    CORE --> FW
    PCB --> CHECK
    FW --> CHECK
    CHECK --> EVOLVE
    EVOLVE --> CORE
    
    CLI --> CORE
    CLI --> PCB
    CLI --> FW
    CLI --> CHECK
    CLI --> EVOLVE
    
    style ACQ fill:#4a9eff,color:#fff
    style LIFT fill:#4a9eff,color:#fff
    style MMIO fill:#4a9eff,color:#fff
    style BEH fill:#4a9eff,color:#fff
    style COMP fill:#4a9eff,color:#fff
    style GENOME fill:#4a9eff,color:#fff
    style KNOW fill:#4a9eff,color:#fff
    style GENERATE fill:#4a9eff,color:#fff
    style CLI fill:#ff6b6b,color:#fff
    style CORE fill:#ffd43b,color:#000
    style PCB fill:#69db7c,color:#000
    style FW fill:#69db7c,color:#000
    style CHECK fill:#69db7c,color:#000
    style EVOLVE fill:#69db7c,color:#000
    style DB fill:#adb5bd,color:#000
```

## Dependências entre Crates

```mermaid
flowchart LR
    subgraph B.A.S.E. Workspace
        CLI[base-cli] --> CORE[base-core]
        CLI --> PCB[base-pcb]
        CLI --> FW[base-fw]
        CLI --> CHECK[base-check]
        CLI --> EVOLVE[base-evolve]
        
        CORE --> PCB
        CORE --> FW
        
        PCB --> CHECK
        FW --> CHECK
        
        CHECK --> EVOLVE
    end
    
    subgraph External
        SPECTER[specter-probe]
        KICAD[kicad-cli]
    end
    
    CLI --> SPECTER
    PCB --->|executa| KICAD
```

## Tecnologias por Crate

| Crate | Language | Deps Externas | Output |
|-------|----------|--------------|--------|
| base-core | Rust | serde, petgraph, clap | YAML/JSON |
| base-pcb | Rust | serde | .kicad_sch, .kicad_pcb, BOM |
| base-fw | Rust | serde | .c, .rs, Makefile |
| base-check | Rust | serde, csv, plotters | .html, .json |
| base-evolve | Rust | serde | .yaml |
| base-cli | Rust | clap, base-* | CLI |
