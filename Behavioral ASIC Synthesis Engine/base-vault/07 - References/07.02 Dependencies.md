---
tags:
  - reference
  - dependencies
---

# Dependencies

## B.A.S.E. Workspace

```toml
[workspace]
members = [
    "base-core",
    "base-pcb",
    "base-fw",
    "base-check",
    "base-evolve",
    "base-cli",
]
```

## Crate Dependencies

### base-core
```toml
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
petgraph = "0.7"
sha2 = "0.10"
hex = "0.4"
tracing = "0.1"
```

### base-pcb
```toml
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
chrono = "0.4"
base-core = { path = "../base-core" }
```

### base-fw
```toml
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
tracing = "0.1"
base-core = { path = "../base-core" }
```

### base-check
```toml
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csv = "1"
chrono = "0.4"
plotters = { version = "0.3", features = ["evcxr", "all_series"] }
tracing = "0.1"
base-core = { path = "../base-core" }
```

### base-evolve
```toml
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
tracing = "0.1"
base-core = { path = "../base-core" }
```

### base-cli
```toml
[dependencies]
clap = { version = "4", features = ["derive"] }
base-core = { path = "../base-core" }
base-pcb = { path = "../base-pcb" }
base-fw = { path = "../base-fw" }
base-check = { path = "../base-check" }
base-evolve = { path = "../base-evolve" }
```
