---
tags:
  - v2
  - feedback
---

# Fase 9 — Closed Feedback Loop

**Esforço:** 4 semanas | **Risco:** Médio | **Inovação:** ★★★

> Pipeline recursivo, não linear. Cada erro melhora o modelo. O ciclo nunca termina.

## Conceito

```text
Analyze → Model → Synthesize → Validate → Error → Refine → Analyze
   │                                                       │
   └────────────────────── LOOP ◄──────────────────────────┘
```

## Tarefas

- [ ] Pipeline recursivo completo
- [ ] Módulo de diagnóstico de erros (classifica falhas)
- [ ] Refinamento automático do modelo baseado em erros
- [ ] Loop contínuo: `base reconstruct --watch`
- [ ] Métricas de convergência por iteração

## Diagnóstico de Erros

```rust
enum ErrorClass {
    MissingRegister { offset: u32 },
    WrongTiming { event: String, expected: Latency, actual: u64 },
    UnmappedAddress { address: u64 },
    MissingInterrupt { vector: u8 },
    ExtraEvent { description: String },
}
```

## Refinamento Automático

```rust
fn refine_model(model: &mut BirDevice, errors: &[DiagnosedError]) {
    for error in errors {
        match error {
            ErrorClass::MissingRegister { offset } => {
                // Adiciona registro desconhecido ao modelo
                model.registers.push(BirRegister {
                    offset: *offset,
                    name: format!("unknown_{:x}", offset),
                    confidence: 0.3, // baixa confiança, refinável
                    ..default()
                });
            }
            ErrorClass::WrongTiming { event, .. } => {
                // Relaxa contrato temporal
                if let Some(contract) = model.contract_mut(event) {
                    contract.latency.max *= 2; // dobra tolerância
                }
            }
            _ => {}
        }
    }
}
```

## Métricas de Convergência

```text
Iteração 1: pass rate = 45% — 12 erros encontrados
Iteração 2: pass rate = 67% — 8 erros encontrados
Iteração 3: pass rate = 83% — 4 erros encontrados
Iteração 4: pass rate = 91% — 2 erros encontrados
Iteração 5: pass rate = 94% — CONVERGIU (threshold: 90%)
```

```bash
# Cap elevado (1000); ainda para em converge/estagnação — NÃO loop infinito / auto-fix
base reconstruct hardware_spec.yaml --continuous -o out/recon
base reconstruct hardware_spec.yaml --max-iterations 10 -o out/recon
base reconstruct hardware_spec.yaml --threshold 0.95 -o out/recon
```

> Nota v0.7 (W3): ver [[17 - Path to v0.7/17.30 - Reconstruct Continuous]]. `--watch` continua fora de escopo.

[[09.00 - Index]]
